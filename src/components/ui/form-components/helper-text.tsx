import { HelperText } from './form-components.styled';

export function FormHelperText({ children }: { children: React.ReactNode }) {
  return <HelperText>{children}</HelperText>;
}

export default FormHelperText;
